# Development & Linting
ruff
pytest
pytest-dotenv
python-dotenv

# Core & Data Models
pydantic

# Vector Database & Math
# Use faiss-cpu for pip install; faiss-gpu available via conda on Linux
faiss-cpu
numpy

# Embedding Backends
voyageai>=0.2.0
sentence-transformers>=2.2

# Data Fetching & Datasets
datasets
Pillow
