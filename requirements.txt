# Development & Linting
ruff
pytest
pytest-dotenv
python-dotenv

# Core & Data Models
pydantic

# Vector Database & Math
# FAISS GPU: Installed in Docker via pip (faiss-gpu-cu12)
# Windows native: Use faiss-cpu (no GPU wheels available)
# Linux native: pip install faiss-gpu-cu12
numpy

# PyTorch with CUDA 12.4 support
# IMPORTANT: Must be installed BEFORE sentence-transformers to ensure GPU acceleration
# CPU fallback is handled in Dockerfile when CUDA is unavailable
# Install GPU version: pip install torch --index-url https://download.pytorch.org/whl/cu124
# Install CPU version: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.5.0

# Embedding Backends
voyageai>=0.2.0
sentence-transformers>=2.2

# Data Fetching & Datasets
datasets
Pillow
tqdm

# LLM Backends
openai>=1.0.0

# HTTP Client (for Kroki rendering)
httpx>=0.25.0

# MCP Server
fastmcp>=2.0,<3
pytest-asyncio>=0.23

